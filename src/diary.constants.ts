// Single personal Telegram chat that owns the /diary web app and the
// notifications/notes primary target. Keep in sync across diary-api,
// notifications-api, notes-api and the own-reel → diary bridge.
export const DIARY_CHAT_ID = 150847737n;
export const DIARY_CHAT_ID_NUMBER = 150847737;
